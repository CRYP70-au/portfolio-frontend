const contractAddresses = require("./contractAddresses.json");
const abiSwap = require("./swap-abi.json");
const abiToken = require("./token-abi.json");

module.exports = {
    contractAddresses,
    abiSwap,
    abiToken
}