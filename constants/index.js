const contractAddresses = require("./simpleswap/contractAddresses.json");
const abiSwap = require("./simpleswap/swap-abi.json");
const abiToken = require("./simpleswap/token-abi.json");

module.exports = {
    contractAddresses,
    abiSwap,
    abiToken
}